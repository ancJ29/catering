export type InternalDetail = {
  id: string;
  materialId: string;
  amount: number;
  actualAmount: number;
  price: number;
  actualPrice: number;
  kitchenDeliveryNote: string;
  internalNote: string;
  isChecked: boolean;
  expiryDate: number;
};
