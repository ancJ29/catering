export type OrderDetail = {
  id: string;
  materialId: string;
  amount: number;
  actualAmount: number;
  price: number;
  actualPrice: number;
  supplierNote: string;
  internalNote: string;
  vat: number;
};
